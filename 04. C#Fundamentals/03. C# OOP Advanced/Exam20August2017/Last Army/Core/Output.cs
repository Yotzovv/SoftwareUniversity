using System.Text;

public static class Output
{
   public static string GiveOutput(IArmy army, IWareHouse wareHouse, int missionQueueCount)
   {
        StringBuilder result = new StringBuilder();
        int totalAmmunitions = 0;

        result.AppendLine("Results:");
   }
}